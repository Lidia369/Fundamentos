let a = [3,5,7,9];
class vector 
    {
        constructor(n,m)
            {
                this.num = [2,4,6,8]
            }
        
        // constructor(n,m)
        //     {
        //         for(let i = 0; i<m;i++){

        //         }
        //     }

        add(a)
            {
                
                if (a.lenght == vector.num.lenght){

                    return a + vector.num.lenght // se puede poner un bucle para ir uno a uno y sumar las posiciones

                }


            }

        productNum(n)
            {
                return vector.num * n
            }

        subs(a)
            {
                // Lo mismo que en el add (a)
            }

        product(a)
            {
                //Lo mismo que el de suma
            }
    }